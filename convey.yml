tasks:
  import:
    type: docker/import
    files: .:.
  build:
    type: docker/build
    tag: rwgrim/msys2-cross:latest
    dockerfile: Dockerfile
    files: .:.

plans:
  default:
    stages:
      - tasks:
         - import
         - build

